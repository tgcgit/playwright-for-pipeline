{
  "name": "playwright-ts-pipeline",
  "version": "1.0.1",
  "description": "Default Playwright Project for pipeline execution",
  "main": "index.js",
  "scripts": {
    "test": "npm install && npx playwright install --with-deps && npx playwright test"
  },
  "keywords": [],
  "author": "Dr. Chikkanna",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1"
  }
}
